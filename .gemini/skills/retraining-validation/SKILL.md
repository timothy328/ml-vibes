---
name: retraining-validation
description: Validate candidate models before API promotion.
---

# Retraining validation

Check the feature and target schema, training-only preprocessing, isolated
tuning data, untouched holdout evaluation, unit tests, default pipeline run,
and API scoring behavior. Compare candidate and champion metrics and document
the promotion decision. Keep the champion if any check fails.

